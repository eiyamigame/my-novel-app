import streamlit as st

# 1. ตั้งค่าหน้าแอปและธีมม่วง-ดำ
st.set_page_config(page_title="Eve's Story Forge", page_icon="🍷")

st.markdown("""
    <style>
    /* พื้นหลังสีดำและตัวหนังสือสีม่วงอ่อน */
    .stApp { background-color: #0b0b0b; color: #bf94ff; }
    
    /* ปุ่มกดสีม่วงเข้มเข้ม */
    .stButton>button { background-color: #7b2cbf; color: white; border-radius: 15px; border: none; width: 100%; font-weight: bold; }
    .stButton>button:hover { background-color: #9d4edd; color: white; }
    
    /* หัวข้อสีม่วงนีออน */
    h1, h2, h3 { color: #9d4edd !important; text-shadow: 2px 2px 4px #000000; }
    
    /* ช่องกรอกข้อมูล */
    .stTextInput>div>div>input, .stTextArea>div>div>textarea { background-color: #1a1a1a; color: #bf94ff; border: 1px solid #7b2cbf; }
    
    /* แถบเมนูด้านข้าง */
    [data-testid="stSidebar"] { background-color: #050505; border-right: 1px solid #3c096c; }
    </style>
    """, unsafe_allow_html=True)

# 📑 เมนู Sidebar
with st.sidebar:
    st.title("💜 Eve's Workspace")
    menu = st.radio("เลือกภารกิจ:", ["🏠 หน้าหลัก", "👤 คลังตัวละคร (วัดโบ้)", "✍️ ห้องปั่นนิยาย"])

if menu == "🏠 หน้าหลัก":
    st.title("📔 Eve's Story Forge")
    st.write("ยินดีต้อนรับสู่คลังนิยายสีม่วง-ดำครับคุณอีฟ! ดูแพงและลึกลับมากครับ! ครับ!")

elif menu == "👤 คลังตัวละคร (วัดโบ้)":
    st.title("👤 สถานะตัวละคร")
    name = st.text_input("ชื่อตัวละคร", placeholder="เช่น ออสติน")
    bo_level = st.select_slider("ระดับความโบ้", options=["ดื้อรั้น", "เริ่มหวั่นไหว", "ยอมสยบ", "โบ้ 100%"])
    if st.button("บันทึกข้อมูล"):
        st.success(f"บันทึกข้อมูล {name} เรียบร้อย! ระดับโบ้: {bo_level}")

elif menu == "✍️ ห้องปั่นนิยาย":
    st.title("✍️ ละเลงเนื้อเรื่อง")
    st.text_input("ชื่อตอน")
    st.text_area("ปลดปล่อยจินตนาการตรงนี้เลยครับ...", height=400)
    if st.button("เซฟเนื้อหา"):
        st.balloons()
      
